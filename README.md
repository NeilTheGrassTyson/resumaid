# resumaid

Personal job-search copilot — discovers roles from permitted APIs, tailors resumes
and cover letters, and queues every application for human review before it's sent.

It never submits an application on its own. See `CLAUDE.md` for the constraints
that govern the project.
